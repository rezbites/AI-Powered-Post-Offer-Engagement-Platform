# --- Web framework --------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Validation & settings ------------------------------------------------
# Pydantic v2 is the backbone of this project: it validates HTTP boundaries
# AND parses/validates LLM structured output through the same type system.
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# --- Database -------------------------------------------------------------
sqlalchemy[asyncio]==2.0.36
alembic==1.14.0
asyncpg==0.30.0
aiosqlite==0.20.0          # SQLite fallback when Docker is unavailable
greenlet==3.1.1            # required by SQLAlchemy async

# --- LLM ------------------------------------------------------------------
google-genai==0.3.0

# --- Security -------------------------------------------------------------
python-jose[cryptography]==3.3.0
passlib[argon2]==1.7.4
python-multipart==0.0.20

# --- Scheduling -----------------------------------------------------------
apscheduler==3.11.0

# --- Observability --------------------------------------------------------
structlog==24.4.0
prometheus-client==0.21.1

# --- Seeding --------------------------------------------------------------
faker==33.1.0

# --- Testing --------------------------------------------------------------
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
